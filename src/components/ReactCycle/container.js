import ReactCycleParent from './ReactCycleParent';
import { connect } from 'react-redux';

const mapStateToProps = (() => {
    return {}
})

export default connect(mapStateToProps,null)(ReactCycleParent);
